const defaultState = {
  // For demo purposes only. You can remove this.
  foo: 'bar'
};

const trains = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default trains;
