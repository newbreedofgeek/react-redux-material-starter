const defaultState = {
  userList: ['bar', 'tim', 'bob', 'jack']
};

const users = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default users;
